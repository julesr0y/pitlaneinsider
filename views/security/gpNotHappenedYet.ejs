<!DOCTYPE html>
<html lang="<%= __('lang.lang') %>">
    <head>
        <%- include('../partials/head.ejs') %>
        <%- include('../partials/header.ejs') %>
        <title>PLI - <%= __('security.notHappened.tab_title') %></title>
    </head>

    <body class="font-F1Regular text-<%= theme %> bg-b-<%= theme %> m-0">
        <main>
            <div class="flex justify-center items-center pt-4">
                <div class="text-center">
                    <p class="font-F1Bold text-2xl"><%= __('security.notHappened.not_happened') %></p>
                    <a href="/calendar" class="text-blue-500 hover:text-blue-800 text-lg"> <%= __('security.notHappened.calendar') %> </a>
                </div>
            </div>
        </main>
        <%- include('../partials/footer.ejs') %>
    </body>
</html>
